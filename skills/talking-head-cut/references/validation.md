# 实际验证依据

以下为维护者 2026-09-15 的历史验证摘要。原片、完整工程及本机证据未随仓库公开；这些记录不是下载者可直接复现的验收结果。

证据文件位于 `维护者本机（未公开）`：`skill-validation.md`、`qc-summary.md`、`qc.json`、`reproduction/`、`reference-comparison/same-sentence-comparison.jpg`。后续对照说明来自该任务完成消息，不能把任务自述扩大成此次重新视听结论。

已产出 149.44 秒、52 个保留段、111 页字幕。该任务报告文件解码、画面映射、波形同步、字库/宽度和响度数值通过；全片听审、51 个切口自然度、逐字校对及重点声学落点未做，仍是 review_required。

本次修订对应的问题：

| 已复现问题 | 修改入口 |
|---|---|
| 提示/重拍合成高概率长词 | speech_checks.py asr＋editorial.md 短窗复识别 |
| 金额/专业词被按字数拆卡 | caption_pages.py＋可覆写逐页词 ID 计划 |
| 全局关键词漏掉否定、数字与行动 | takeaway→完整重点→focus/structure 逐句决策，不再统一名词变色 |
| 固定单行字号、没有顶部主题和列表 | editorial.md 的主题、分层与列表规则 |
| 直接封装产生约 42.7ms 固定延迟 | 编码后波形同步工具与校准阶段检查；不硬编码历史修正值 |
| 没有实际听审通道 | 提前记录能力并生成逐切口接管清单；不豁免视听门槛 |

新脚本的本地测试结果保存在当前任务的 `维护者本机的 skill升级验证 目录（未公开）`。Mock API 测试证明请求、轮询、恢复和下载分支的实现，不证明百炼服务或模型效果。

升级后新增百炼 Paraformer 转写、源词→成片词映射及内置字体。28 项本地测试通过；8 秒真实 raw 摘取片段完成 200 帧历史渲染验证，三套字体有输出帧。发现并修复了同步检测中未归一化的相关性算法误判。详细证据、前后音轨检查及限制见该目录 `验证报告.md` 与 `verification.json`；仍不代表独立无上下文整片质量复验已通过。

用户随后将紧凑接句目标定为50ms左右，并要求两行滚动字幕。本轮51个已剪接句点的ASR间隔从平均147.06ms/中位160ms降至46.67ms/50ms；实际导出144.32秒气口比较版和20秒滚动字幕样例。结果在 `维护者本机的气口与字幕调整说明（未公开）` 与 `qc.json`。这组统计不包含句内空档，不能外推所有呼吸均50ms；完整听审仍未完成。

2026-09-15 后续用户要求按句内/展开/完整句/话题预标气口，再剪媒体，并调整样例3–4秒接句。本次新增 `semantic_pacing.py` 与四档20/30/60/120ms紧凑预设；全部70候选（51接句＋19句内）均已逐处标注。与上一版同口径的68处正间隔比较：均值82.35→42.79ms，中位60→25ms。指定接句按ASR词边界从60→30ms，新样片时间3.000–3.030秒；不是声学纯静音识别结果。

证据在 `维护者本机的语义气口与字体目录（未公开）`：`pause-decisions.json`、`qc.json`、`preview-20s.mp4`、SRT和可编辑工程。全片候选声音时间轴141.63秒、848词保留，847个有时长词的PCM与原保留音频逐采样相同；零时长词及相邻140ms空档保留待审。只重渲染20秒/500帧1080×1920样片，整片新版画面未渲染，完整听审仍not_checked。28项脚本测试通过，样片解码、字幕字宽、画面映射及编码后波形同步通过。

主Skill新增 Google Fonts 官方固定commit的 Noto Sans SC、庆科黄油、马善政楷书、站酷快乐四款原始字体及OFL许可。四款对本片所有字幕字符的cmap检查无缺字；样片实际用Noto400正文＋庆科黄油400重点，字体对照图展示全部四款。不能把本片字符覆盖推广为任意生僻字均覆盖。
